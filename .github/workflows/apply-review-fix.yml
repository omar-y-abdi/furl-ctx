name: Apply PR review fix

on:
  push:
    branches:
      - agent/integrate-context-safety-improvements

permissions:
  contents: write

jobs:
  apply:
    if: github.event.head_commit.message == 'stage PR review fix applicator'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: agent/integrate-context-safety-improvements
          fetch-depth: 1
      - name: Validate and apply review fix
        shell: bash
        run: |
          set -euo pipefail
          git apply --check .review-fix.patch
          git apply .review-fix.patch
          git rm .review-fix.patch .github/workflows/apply-review-fix.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add furl_ctx tests
          git commit -m 'fix: address Codex review findings'
          git push origin HEAD:agent/integrate-context-safety-improvements
