# lazy-dev-AUDIT-v2.md

> Generated by the `simplify-audit-v2` workflow (run wf_c4d4246b-f17): 10 tool-grounded read-only lenses x loop-until-dry (3 rounds) -> perspective-diverse verify -> completeness critic. 281 agents, 12.7M tokens. 239 findings, 230 survivors, 0 net-refuted. REPORT-ONLY.

---

# Headroom Simplification Audit v2 (lazy-senior, tool-grounded)

REPORT-ONLY. Branch `verify/phase2-audit-report`. 230 reachability-checked findings, deduped across 13 lenses.

## Exec Summary (5 lines)
1. **Cuttable NOW (Tier 1, verified dead, no untangle):** ~2,640 LOC + ~1.45 MB stale JSON + the (already-done) 19 GB worktree. **After untangle (Tier 2):** ~+6,600 LOC more (incl. the v2 headline live_zone subsystem).
2. **Biggest disk lever:** `.claude/worktrees/` was 19 GB / 8 dead worktrees — **VERIFIED ALREADY CLEANED (now 0 B, no dangling git registration).** Caused the ENOSPC during this run; resolved.
3. **Biggest CODE lever:** `live_zone.rs` whole-request compression subsystem (~2,000 of 2,899 LOC) — reachable only from its own `#[cfg(test)]` + a dead PyO3 bridge. **VERIFIED:** `compress_openai_responses_live_zone` has zero Python importers; live product path is `content_router` → per-type Rust compressors. Tier 2 (shared types are load-bearing — must hoist first).
4. **Panel net-refuted 0 findings.** No finding carries `survives:false`. ~15 carry a lone dissenting "refuted" vote but survived; all flagged "(needs review)". The Refuted appendix therefore contains zero net-drops (honest).
5. **Lenses NOT-RUN / coverage gaps:** Cannot be determined from the finding set — the JSON exposes only lenses that *produced* findings (deadcode-py, deadcode-rust, duplication, dep-bloat, over-abstraction, abstraction-bloat, dead-config, api-surface, test-bloat, archaeology, doc-cruft, dup-py-rust, generated-artifacts). No lens roster was provided, so "ran-but-clean" vs "never-ran" is indistinguishable. **This is NOT a zero-fat signal — treat as unknown coverage: [unknown — no roster].**

---

## TIER 0 — NON-CODE DISK / ARCHAEOLOGY

| # | What | Paths | ~Size | Tag | Lens | Evidence |
|---|------|-------|-------|-----|------|----------|
| 0.1 | 19 GB dead worktrees — **DONE** (verified 0 B, no dangling reg) | `.claude/worktrees/` | was 19 GB | archaeology | deadcode-py, archaeology | `du -sh`=0B; `git worktree list`=current only; `prune -n`=no candidates |
| 0.2 | Committed eval/bench run-**OUTPUTS** (regenerable, 0 consumers) | `verify/raw_results.json` (440 K), `verify/heldout/raw_results.json` (782 K), `benchmarks/{baseline,final}_results.json` | ~1.45 MB | delete | generated-artifacts(critic) | timestamped+commit-stamped outputs; `rg` confirms 0 code reads them as inputs. `benchmarks/data/*.raw.json` are the audited INPUT source-of-truth — EXCLUDED |
| 0.3 | Committed demo/doc media (25.6 MB binary) | `Headroom-2.gif` 5.4M, `headroom_learn.gif` 14M, `HeadroomDemo-Fast.gif` 4.4M, `headroom-savings.png` 1.1M, `wiki/screenshots/*` | 25.6 MB | shrink | archaeology | `git ls-files`. **(needs review — still referenced by README/wiki; load-bearing until docs move to CDN)** |
| 0.4 | Empty phantom dirs (untracked scaffolding) | `headroom/memory/adapters`, `headroom/memory/backends` | 0 LOC | delete | archaeology(critic) | `find`=empty, `git ls-files`=0. `rmdir` only |
| 0.5 | `MEDIUM FIX #N:` audit-cycle comment markers (fixes already applied) | telemetry/models.py, toin.py, cache/compression_store.py, compression_feedback.py (18 sites) | 18 lines | archaeology | doc-cruft | `rg 'MEDIUM FIX'`=18; strip prefix only |
| 0.6 | Doc cruft: stale API docs (HeadroomClient / SharedContext / RollingWindow / removed CLI) | see Doc-Cruft block below | ~2,800 doc lines | archaeology | doc-cruft, api-surface | grouped below |

**Dual lockfiles (Cargo.lock + uv.lock):** NOT duplication — Cargo.lock for Rust crates, uv.lock canonical for Python (`[tool.uv]`). Both correct. No action.

### Tier 0 doc-cruft (grouped — all dead, surgical or whole-file)
| # | What | Path | ~LOC | Action |
|---|------|------|------|--------|
| D1 | `wiki/sdk.md` documents removed `HeadroomClient` | wiki/sdk.md | 226 | rewrite to `compress()` |
| D2 | `wiki/api.md` HeadroomClient/RollingWindowConfig/IntelligentContextConfig/ScoringWeights/simulate | wiki/api.md | ~280 | rewrite, keep CompressConfig/CompressResult |
| D3 | `wiki/configuration.md` stale client/proxy-flags/per-request-overrides/simulate | wiki/configuration.md | ~130 | surgical delete |
| D4 | `wiki/ARCHITECTURE.md` File-Structure (10+ nonexistent files), /v1/retrieve+/v1/feedback, HeadroomClient diagram | wiki/ARCHITECTURE.md | ~220 | surgical delete |
| D5 | `wiki/transforms.md` RollingWindow + IntelligentContextManager as live (retired PR-B1) | wiki/transforms.md + ccr/proxy/api.md | ~260 | mark RETIRED |
| D6 | HeadroomClient mass-stale cluster (9 wiki files + 3 src docstrings) | exceptions.py:1-17, config.py:463, transforms/pipeline.py:220 + wiki | ~850 | rewrite/fix |
| D7 | `wiki/shared-context.md` whole file (SharedContext archived) + README:182,198 | wiki/shared-context.md | ~112 | delete file + README lines |
| D8 | `wiki/network-diff-capture.md` whole file (archived docker + nonexistent CLI) | wiki/network-diff-capture.md | 66 | delete |
| D9 | `wiki/plans/dynamic-smart-crusher.md` orphaned plan (already implemented: adaptive_sizer.py) | wiki/plans/dynamic-smart-crusher.md | 575 | archive/move. **(needs review)** |
| D10 | README: ENTERPRISE.md dead link (only in archive/) | README.md:30 | 1 | fix link |
| D11 | README: pipeline-internals lists 6 nonexistent files | README.md:215-219 | 5 | rewrite |
| D12 | `llms.txt` `headroom wrap gemini` — command doesn't exist | llms.txt:24 | 1 | remove `gemini` |
| D13 | Inconsistent savings claims (README/llms.txt 60-95%, __init__ 50-90%, pyproject 50-90%, wiki/index 40-90%) vs BENCHMARKS.md authoritative 0-54% | README.md, llms.txt, __init__.py:4, pyproject.toml:8, wiki/index.md:77 | ~6 | align to honest range |
| D14 | `wiki/index.md` 87% stat + SharedContext example | wiki/index.md:356-364 | 10 | fix |
| D15 | `SemanticCacheLayer` usage example in cache/__init__ docstring (never implemented) | cache/__init__.py:13-20 | 8 | delete docstring block |

**ERA001 reconciliation:** Only `compression_store.py:1097` is a genuine commented-out-code hit (real cut, in Tier 1). The other 6 ERA001 sites (`tiktoken_counter.py:53,57` dict section headers; `models.py:292` priority-order comment; `toin.py:659` else-branch rationale; `batch_processor.py:220`, `response_handler.py:347` format-doc comments) are **false positives** — add `# noqa: ERA001`, NOT a cut. Per-line evidence beats the generic ERA001=dead lens.

---

## TIER 1 — SAFE NOW (verified dead/vestigial, no untangle)

Sorted biggest-cut-first. Rows flagged `not_verified`/`needsReview` carry "(needs review)" — not green-lit.

| Rank | What | Paths | ~LOC | Tag | Lens | Evidence |
|------|------|-------|------|-----|------|----------|
| 1 | `proxy/helpers.py` context-tool stats cluster — never called | proxy/helpers.py:910-1441 | 530 | delete | deadcode-py | `rg` 18 fn names → 0 hits outside helpers.py (re-verified: 0). panel ✓✓✓ |
| 2 | `proxy/helpers.py` tool-search hint cluster — never called | proxy/helpers.py:2742-2932 | 190 | delete | deadcode-py | `rg` 7 fn names → 0 hits (re-verified: 0). panel ✓✓✓ |
| 3 | `conftest.py` 15 dead fixtures (incl. import of nonexistent `providers.openai`) | tests/conftest.py:71-265 | 193 | delete | test-bloat (×2) | `rg` all 15 fixtures → 0 injection sites. Keep lines 1-70 (live hook + autouse). panel ✓✓✓ |
| 4 | `component_tracker` MemoryTracker singleton + ProcessStats + MemoryReport orphaned | component_tracker.py:167-347 | 180 | delete | abstraction-bloat | `MemoryTracker.register` never called in prod. panel ✓✓✓ |
| 5 | `utils.py` 11 dead functions | utils.py:18,35,67,74,102,109,138,145,154,180,196,238 | 110 | delete | deadcode-py | vulture@60 + grep: only `extract_user_query` imported. Keep 7 live fns. panel ✓✓~ |
| 6 | `TelemetryCollector.record_compression()` — prod routes via TOIN | telemetry/collector.py:101-202 | 100 | delete | abstraction-bloat | only docstring example calls it; prod → `toin.record_compression`. panel ✓✓✗ **(needs review)** |
| 7 | `find_tool_units()` — public fn, no callers | parser.py:350-431 | 82 | delete | deadcode-py | `rg`→def only. panel ✓✓✓ |
| 8 | `MemoryTracker` (dup row) / `estimate_object_size()` recursive-only | component_tracker.py:350-383 | 34 | delete | abstraction-bloat | only self-recursive calls. panel ✓✓✓ |
| 9 | `_persist_to_python_ccr` triple-duplication → extract shared fn | log_compressor.py:524, search_compressor.py:338, diff_compressor.py:129 | 35 | dup | duplication | bodies char-identical (27 LOC ×3). panel ✓✓✓ |
| 10 | `TokenizerRegistry` admin methods (register_backend/list_backends/list_registered) | tokenizers/registry.py:200-230 | 30 | delete | abstraction-bloat, deadcode-py | `rg`→def only, no prod/test callers. panel ✓✓✓ |
| 11 | `_reset_*_for_test` ×3 stubs — no test uses them | proxy/helpers.py:1828,2109,2485 | 30 | delete | deadcode-py | `rg`→0 hits in any test. panel ✓~✗ **(needs review)** |
| 12 | `CharacterCounter` test-only (superseded by `EstimatingTokenCounter(chars_per_token=)`) | tokenizers/estimator.py | 35 | yagni | abstraction-bloat | only in test_tokenizers.py. panel deferred **(needs review)** |
| 13 | `unload_kompress_model()` — no callers | transforms/kompress_compressor.py:651 | 28 | delete | deadcode-py (×2) | not exported, `rg`→def. panel ✓~✗ **(needs review)** |
| 14 | `get_huggingface/mistral_tokenizer` + `is_mistral_tokenizer_available` test-only wrappers | tokenizers/__init__.py | 20 | yagni | abstraction-bloat | only in test_tokenizers.py. **(needs review)** |
| 15 | `default_batch_score()` free fn — zero callers | relevance/base.py:91-106 | 17 | delete | abstraction-bloat (×2) | `rg`→def only. panel ✓✓✓ |
| 16 | `transforms/base.py split_frozen` — never called | transforms/base.py:12-27 | 16 | delete | deadcode-py | not in `__all__`/exports. panel ✓✓✓ |
| 17 | `get_message_content_text()` — no callers | parser.py:433-448 | 16 | delete | deadcode-py | `rg`→def. panel ✓✓✓ |
| 18 | `SimulationResult` orphan (removed simulate API) | config.py:636-649, __init__.py:79,151 | 15 | delete | api-surface | `rg 'SimulationResult('`→0. panel ✓✓✓ |
| 19 | `TokenizerRegistry.list_backends/list_registered` | tokenizers/registry.py:215-235 | 15 | delete | deadcode-py | `rg`→0. panel ✓✓✓ |
| 20 | `is_json_array_of_dicts` (Python) | transforms/content_detector.py:421 | 12 | delete | deadcode-py | not exported. panel ✓✓✓ (Rust `_core` half also dead — see T2) |
| 21 | `find_content_router()` — no call sites | transforms/compression_units.py:114-123 | 10 | delete | deadcode-py | `rg`→def. panel ✓✓✓ |
| 22 | `APIClient` Protocol stub — unused | ccr/batch_processor.py:37-47 | 10 | delete | deadcode-py, over-abstraction | uses httpx.AsyncClient directly. panel ✓✓✓ |
| 23 | `CCRResponseHandler.get_stats()` — never called externally | ccr/response_handler.py:525-533 | 9 | delete | deadcode-py | mcp_server calls different object. panel ✓✓✓ |
| 24 | `reset_telemetry_collector` — exported, never called | telemetry/collector.py | 5 | delete | abstraction-bloat | `rg`→def/export. panel ✓✓✓ |
| 25 | `get_huggingface_tokenizer()` (tokenizers/__init__) | tokenizers/__init__.py:37 | 5 | delete | deadcode-py | `rg`→def. panel ✓~✓ |
| 26 | `mcp_server` `direct_mode` no-op param | ccr/mcp_server.py:905 | 3 | deadflag | deadcode-py, duplication | vulture 100%; "Ignored (kept for compat)". panel ✓✓✓ |
| 27 | `_telemetry_noop` unused `attributes` param | transforms/_telemetry_noop.py:47 | 1 | delete | duplication | vulture 100%. **(needs review — verify not protocol-required)** |
| 28 | `DEFAULT_TOIN_DIR`/`DEFAULT_TOIN_FILE` constants | telemetry/toin.py:84-85 | 2 | delete | deadcode-py (×2) | paths.py uses own `_TOIN_FILE`. panel ✓✓✓ |
| 29 | `HuggingFaceTokenizer.is_available/list_supported_models` classmethods | tokenizers/huggingface.py:293-325 | 25 | delete | deadcode-py | `rg`→0 external. panel ✓✓✓ |
| 30 | `EstimatingTokenCounter.count_messages()` pure-delegate no-op | tokenizers/estimator.py:144-157 | 13 | shrink | abstraction-bloat | body = `return super().count_messages()`. **(needs review)** |

### Tier 1 lint-hygiene (RUF100/ERA001/F401/PLR1711 — real dead-code/hygiene, NOT complexity churn)
| # | What | Paths | ~LOC | Lens | Evidence |
|---|------|-------|------|------|----------|
| L1 | 19 stale `noqa: F401` (RUF100) — TYPE_CHECKING blocks now recognized | cache/__init__.py:34-55, transforms/__init__.py:11-64 | 0 net | deadcode-py, duplication | `ruff --fix` auto. panel ✓✓✓ |
| L2 | 7 dead test imports (F401) | 5 test files (see JSON) | 7 | test-bloat (×2) | `ruff --select F401`. panel ✓✓✓ |
| L3 | `noqa: BLE001` ×4 (non-enabled rule) | pipeline.py:80,87,94,167 | 4 | deadcode-py | RUF100. panel ✓✓✓ |
| L4 | `noqa: D401` ×2 + `noqa: BLE001` ×2 | _telemetry_noop.py:33,57, content_router.py:1457, cross_message_dedup.py:533 | 4 | archaeology | RUF100. panel ✓✓✓ |
| L5 | `noqa: ARG002` ×2 + useless `return None` (PLR1711) | toin.py:960,961,989 | 3 | deadcode-py | RUF100 + PLR1711. panel ✓✓✓ (one row ✓✓✗ **needs review**) |
| L6 | `importorskip_no_env_leak` helper unused | tests/_dotenv.py:75-106 | 32 | delete | test-bloat | `rg`→def only. panel ✓✓✓ |
| L7 | `CompressionProfile.min_k`/`max_k` never read (+ PROFILE_PRESETS kwargs) | config.py:269-270,275-277 | 2 | deadflag | dead-config | only `.bias` consumed. panel ✓✓✓ |
| L8 | `compression_store.py:1097` genuine commented-out code | cache/compression_store.py:1097 | 1 | archaeology | doc-cruft | the one real ERA001 hit |

**Tier 1 dedup'd subtotal: ~2,640 LOC** (proxy clusters 720 + conftest 193 + memory tracker cluster 214 + utils 110 + collector 100 + parser 98 + the long tail). The two largest single rows (proxy/helpers clusters) are independently re-verified at 0 callers.

---

## TIER 2 — CUT AFTER UNTANGLE (dead/vestigial, has a blocking step)

| Rank | What | Paths | ~LOC | Tag | Lens | Exact untangle step |
|------|------|-------|------|-----|------|---------------------|
| 1 | **`live_zone.rs` whole-request compressors (v2 HEADLINE)** — test-only + dead PyO3 bridge | crates/.../transforms/live_zone.rs; headroom-py/src/lib.rs:1540-1655 | ~2,000 | dep | dup-py-rust(critic) | **VERIFIED dead bridge.** 1) Hoist shared `AuthMode`, `LiveZoneError`, `BlockOutcome`, `CompressionManifest` into a small module (reused by recommendations.rs + compression_policy.rs — load-bearing). 2) Delete `compress_anthropic/openai_chat/openai_responses_live_zone` + `dispatch_compressor` + formatters + their `#[cfg(test)]` blocks. 3) Remove PyO3 bridge lib.rs:1540-1655 + registration. 4) `cargo check -p headroom-core`. **Subsumes DispatchResult::Error (12) + responses bridge (70) — counted once.** **(needs review)** |
| 2 | GoogleCacheOptimizer 12 methods (dead via registry chain) | cache/google.py:348-896 | 500 | delete | deadcode-py | Cut `CacheOptimizerRegistry` dead-end first (no prod callers) → Google never retrieved → methods unreachable. Confirm base-class polymorphism doesn't require them. **(needs review)** |
| 3 | `StreamingCCRBuffer` + `StreamingCCRHandler` cluster | ccr/response_handler.py:537-896, ccr/__init__.py:50-51,83-84 | 310 | delete | deadcode-py | Remove `ccr.__all__`/_LAZY_EXPORTS entries first; verify no plugin/entry_point ref. **(needs review)** |
| 4 | `TelemetryBeacon` class (never instantiated) | telemetry/beacon.py:105-345 | 240 | delete | deadcode-py | Confirm no proxy/Rust startup path instantiates it; keep module-level helpers (is_telemetry_enabled etc.). **(needs review)** |
| 5 | `CompressionFeedback` cluster (get_compression_hints / analyze_from_store / get_all_patterns / CompressionHints) | cache/compression_feedback.py:103,399-573 | 175 | delete | deadcode-py (×2) | Module is LIVE (record_retrieval). Delete only these methods in isolation; check cache/__init__ + headroom/__init__ lazy exports. **(needs review)** |
| 6 | `TelemetryCollector.get_recommendations/export_stats` (+import_stats keep if persistence) | telemetry/collector.py:299-462 | 160 | yagni | abstraction-bloat | `import_stats` is called by `_load_from_disk` — keep that; drop the other two. **(needs review)** |
| 7 | Python log/search compressor internal methods (`_parse/_score/_select/_format`) + `tokens_saved_estimate` | transforms/log_compressor.py, search_compressor.py, diff_compressor.py | 120 | delete | deadcode-py | Verify Python fallback path never triggers (vs pure-Rust). `tokens_saved_estimate` properties safe regardless. **(needs review)** |
| 8 | `cache/google.py` 11 manager methods (alt framing of #2) | cache/google.py:423-850 | 120 | delete | deadcode-py | Same registry-chain untangle as #2 — **dedup with #2, do not double-count.** |
| 9 | `CompressionPolicy` Rust module — plumbed, 0 prod callers | crates/.../compression_policy.rs:234-309 | 76 | delete/yagni | deadcode-rust (×2) | Decide PR-F3 fate: if policy stays Python-driven, delete Rust module + lib.rs:6 decl + move test-only methods to `#[cfg(test)]`. `live_zone_compression_enabled()` returns literal `true`. **(needs review)** |
| 10 | `MemoryTracker` class (deadcode-py framing) | component_tracker.py:167-290 | 75 | delete | deadcode-py | Verify no Rust FFI/runtime introspection ref. **dedup with Tier 1 #4 (same subsystem).** **(needs review)** |
| 11 | `Tiered<T>` signal combinator — test-only | crates/.../signals/tiered.rs | 75-80 | yagni | deadcode-rust (×2) | Check roadmap for ML-tier (`SearchCompressor::with_detector`). If abandoned: delete + remove signals/mod.rs:61 re-export. **(needs review)** |
| 12 | `compression_summary.py` `summarize_dropped_items` whole module | transforms/compression_summary.py:20 | 80 | delete | deadcode-py | Verify no entry_point/plugin runtime caller. panel ✓✓✗ **(needs review)** |
| 13 | `cache/base.py` orphaned semantic-cache + prefix-tracker dataclass fields | cache/base.py:112-177 | 40 | delete | deadcode-py | Check no provider reads fields. (semantic_cache_enabled/threshold/ttl subset is Tier-1-able per over-abstraction ✓✓ but bundled here.) **(needs review)** |
| 14 | `relevance/__init__` create_scorer + EmbeddingScorer + embedding_available export surface | relevance/__init__.py:72-130 | 55 | delete | deadcode-py, deadcode-rust | BM25Scorer consumed via direct submodule import (live). Confirm no importlib/getattr lazy use. **(needs review)** |
| 15 | `cache/registry.py` 5 dead methods | cache/registry.py:63-155 | 45-60 | delete | deadcode-py (×2) | Confirm archive/ exclusion permanent; no test/plugin calls. **(needs review)** |
| 16 | `TiktokenCounter/BaseTokenizer encode/decode` override pair | tokenizers/tiktoken_counter.py:234, base.py:294 | 30 | delete | deadcode-py | Check Protocol/ABC contract before removing from base. **(needs review)** |
| 17 | `set/clear_request_compression_store` (SaaS middleware hooks) | cache/compression_store.py:1193,1205 | 14 | delete | deadcode-py | `rg` full repo (not just headroom/) for middleware caller. **(needs review)** |
| 18 | `process_batch_results()` — exported, 0 external callers | ccr/batch_processor.py:542 | 20 | delete | deadcode-py | Remove from ccr/__init__ `__all__`/_LAZY_EXPORTS after. **(needs review)** |
| 19 | `toin.py` `get_recommendation` (deprecated stub returns None) + `get_pattern`/`iter_patterns` (retired aggregator) | telemetry/toin.py:958-989,1145,1169 | 67 | delete | deadcode-py | Confirm no live/external callers; update toin `__all__`. **(needs review)** |
| 20 | `route_and_compress` / `eager_load_compressors` / `pipeline.simulate` convenience wrappers | content_router.py:2901,1791, pipeline.py:470 | 80 | delete | deadcode-py | Not in exports; each panel had 1 refuted vote. **(needs review)** |
| 21 | `cache/anthropic.py` 4 dead cost/ttl methods | cache/anthropic.py:494-516 | 20 | delete | deadcode-py | Check cache/base.py doesn't declare as abstract/protocol. **(needs review)** |
| 22 | Dead `_core` PyO3 surface: `hello`, `is_json_array_of_dicts` registration (separable from #1) | headroom-py/src/lib.rs:1628,1654 | ~9 | delete | dup-py-rust(critic) | `hello` + is_json registration = pure deletes (verified 0 Py importers). Responses bridge folds into #1. |
| 23 | `tool_pair_indices()` / `ToolPair` dead exports | crates/.../transforms/safety.rs, mod.rs:65 | 100 | delete | deadcode-rust | Remove mod.rs:65 re-export; confirm no Python ctypes/cffi call. panel ✓✓~ |
| 24 | `relevance::create_scorer()` Rust factory — 0 callers | crates/.../relevance/mod.rs:49 | 33 | delete | deadcode-rust | SmartCrusher instantiates scorers directly. panel ✓✓~ |
| 25 | `compress_anthropic_live_zone` / `compute_frozen_count` / `auth_mode::classify` crate-root re-exports orphaned post-D7 | crates/.../transforms/mod.rs:48, lib.rs:16, auth_mode.rs:121 | ~6 | archaeology | deadcode-rust | Demote to pub(crate) after checking internal callers. **dedup #1 for the live_zone re-export.** **(needs review)** |
| 26 | Rust test-only pub accessors (chars_per_token, model, encoding_name, registry, has_embedding_support, sqlite_default) | estimator.rs:35, tiktoken_impl.rs:75,79, keyword_detector.rs:236, hybrid.rs:172, ccr/backends/mod.rs:46 | ~25 | delete | deadcode-rust | Add `#[cfg(test)]` guard or inline. Several panels had 1 refuted vote. **(needs review)** |

### Tier 2 — DEAD CONFIG (public-API fields/classes never read; need export/deprecation handling)
| # | What | Paths | ~LOC | Untangle |
|---|------|-------|------|----------|
| C1 | `SmartCrusherConfig` dup in config.py (live one is transforms/) | config.py:294-365 | 72-74 | Re-export transforms version; fix __init__.py:141 import source. **(needs review)** |
| C2 | `CacheAlignerConfig` 8/9 fields dead (only `.enabled` read) | config.py:27-98 | 40-65 | Collapse to single-field or merge into CacheConfig. **(needs review)** |
| C3 | `RelevanceScorerConfig` all fields dead (Rust hardcodes 0.3 / raises NotImplementedError) | config.py:102-145, smart_crusher.py:291,344 | 40-44 | Remove from __init__ exports; SmartCrusherConfig.relevance field too. **(needs review)** |
| C4 | `AnchorConfig` fully dead (AnchorSelector never instantiated) | config.py:145-199,348 | 55 | Bundle with anchor_selector.py removal (T3 below). **(needs review)** |
| C5 | `CacheOptimizerConfig` class phantom (cache uses cache/base CacheConfig) | config.py:368-389,472, __init__.py:72,143 | 24-25 | Remove from __all__; remove HeadroomConfig.cache_optimizer field first. **(needs review)** |
| C6 | `PrefixFreezeConfig` + field — only archive/ references | config.py:441-458,474 | 20 | Confirm archive not deployed → straightforward delete. panel ✓✓✓ (Tier-1-able) |
| C7 | `CCRConfig` 7/9 fields dead (only `.enabled`+`.inject_retrieval_marker` live) | config.py:392-437 | 9-52 | Collapse; wire store params to get_compression_store() if ever needed. **(needs review)** |
| C8 | `HeadroomMode` enum + `default_mode` (never consumed) | config.py:13-19,466 | 10-12 | **Conflict:** doc-cruft says HeadroomMode is a *documented* public enum (langchain.md/agno.md). KEEP enum; `default_mode` field is dead. Remove field only. **(needs review)** |
| C9 | `store_url`, `output_buffer_tokens`, `pipeline_extensions`+`discover_pipeline_extensions`, `cache_optimizer`, `ccr`, `get_context_limit()`, `model_context_limits`, `content_router_enabled` InitVar | config.py:465,479,496-497,474-476,467-469,499-516,480-484 | ~70 | Public-API fields → deprecation cycle. `store_url`/`output_buffer_tokens` panel ✓✓✓ (Tier-1-able). Others need __init__ export cleanup. **(needs review)** |
| C10 | Pipeline entry-point extension seam dead (config fields unread, discover hardcoded off, 0 registrants) | config.py:496-497, pipeline.py:74-100, compress.py:251 | 40 | Delete 2 unread config fields min; remove discover machinery unless plugin ecosystem planned. **(needs review)** |

### Tier 2 — test-file supersession (post-Rust-port; PORT-VERIFY before delete)
| # | What | Path | ~LOC | Untangle |
|---|------|------|------|----------|
| T-T1 | `test_log_compressor.py` (786 LOC, 48 tests) superseded by `test_transforms_log_compressor.py` | tests/test_log_compressor.py | ~607-680 | Enumerate 11 TestXxx classes vs 4 canonical fns; port unique edge/config/dataclass/ratio cases first. **DEDUP: this is ONE cut — the 34-LOC sub-test framing is mutually exclusive, dropped.** **(needs review)** |
| T-T2 | `test_search_compressor.py` (768 LOC, 49 tests) superseded by `test_transforms_search_compressor.py` | tests/test_search_compressor.py | ~619-640 | Same port-verify. ONE cut (34/52-LOC sub-test framings dropped). **(needs review)** |
| T-T3 | `test_text_compressors.py` TestSearchCompressor+TestLogCompressor overlap | tests/test_text_compressors.py:126-242 | 117 | Delete only after T-T1/T-T2 ported; keep TestContentDetector + SmartCrusher integration. **(needs review)** |
| T-T4 | `verify/heldout/measure.py` byte-identical clone of `verify/measure.py` | verify/heldout/measure.py | 879 | Redirect heldout/worker.py + run.py imports to `verify.measure` (4 import lines). **ONE cut (appears twice in JSON).** panel ✓✓✗ **(needs review)** |
| T-T5 | Eval-helper duplication (HEADROOM_RETRIEVE_TOOL, _call_claude, _make_test_* factories) | 3 compression_summary eval files | ~40 | Extract `tests/_eval_helpers.py`; reconcile signatures (tools kwarg, max_tokens). Only worth it if no-assertion tests kept. **(needs review)** |
| T-T6 | No-assertion API-burning baseline tests | integration.py:127, hard_eval.py:158,193, tool_eval.py:193,232 | ~160 | Move to script/notebook OR add assertions — don't leave as cost-burning no-ops. **(needs review)** |

**Tier 2 dedup'd subtotal: ~6,600 LOC** (live_zone ~2000 + google ~500 + streaming 310 + beacon 240 + the config/test/feedback long tail), after folding the documented overlaps (google #2≡#8, MemoryTracker T1#4≡T2#10, live_zone subsumes Error+bridge, measure.py once, test files once).

---

## TIER 3 — SHRINK (live code: yagni / dup / stdlib / delegating-wrapper, low-risk LOC removal)

| Rank | What | Paths | ~LOC | Tag | Lens | Note |
|------|------|-------|------|-----|------|------|
| 1 | **`anchor_selector.py`** full Python impl — reachable only via public `__all__`; live anchoring is Rust (anchor_selector.rs) | transforms/anchor_selector.py | 770 | shrink | deadcode-py(critic) | Remove from transforms/__init__ `__all__`+_LAZY_EXPORTS (lines 11-13,73-74,131-139), confirm no downstream consumer, delete module. Keep `AnchorConfig` (Rust path uses it — but C4 says AnchorConfig dead at HeadroomConfig level; reconcile). **(needs review — live_zone verified, this not)** |
| 2 | Py↔Rust hand-mirror: `compression_policy.py` (276) + `adaptive_sizer.py` (308) duplicate Rust source-of-truth | transforms/compression_policy.py, adaptive_sizer.py + Rust | 0 now / ~580 consolidatable | dup | dup-py-rust(critic) | **LIVE + parity-tested — 0 deletable now.** Consolidate by exposing Rust via PyO3 (as diff/log/search already are); reduce Python to thin wrappers, retire 2 parity suites. Honest: amputated bloat is still load-bearing. **(needs review)** |
| 3 | `Tokenizer` pure-delegating wrapper (7 call sites) + `count_tokens_text/messages` free fns + `.available` property | tokenizer.py | ~67 | yagni | over-abstraction, archaeology | Replace with `BaseTokenizer` alias / `get_tokenizer()`; migrate 7 importers. Consolidate file into tokenizers/ pkg. **(needs review)** |
| 4 | `Provider` ABC — zero concrete impls (amputated) | providers/base.py, __init__.py, transforms/pipeline.py | 95 | yagni | over-abstraction | Strip `provider` param from TransformPipeline.__init__ + dead `_get_tokenizer` branch; remove lazy exports; delete base.py. Changelog (public). **(needs review)** |
| 5 | `CompressionStoreBackend` Protocol — single InMemoryBackend impl | cache/backends/base.py:17-133 | 117 | yagni | abstraction-bloat | Inline or drop Protocol; handle `_create_default_ccr_backend` env hook + constructor `backend` param (tests). **(needs review)** |
| 6 | `exceptions.py` 7 leaf subclasses never raised | exceptions.py:49-192, __init__.py:57-65,129-137 | 130 | dead | api-surface | Remove from __all__/_LAZY_EXPORTS now (safe); physical delete needs deprecation (documented in wiki/errors.md — external catchers). **(needs review)** |
| 7 | `CacheOptimizer` Protocol shadows `BaseCacheOptimizer` ABC — never used as annotation | cache/base.py | 55-60 | dup | over-abstraction, abstraction-bloat | Delete Protocol, keep ABC; remove from __all__ (public, changelog). **(needs review)** |
| 8 | `mistral.py` tokenizer — no install path (mistral_common in 0 extras, always degrades to estimator) | tokenizers/mistral.py + registry plumbing | 270 | deadflag | dep-bloat | Remove mistral branch in `_create_mistral`, registry rows, __init__ exports → mistral.py unreferenced. Cutting only affects someone who manually pip-installed mistral-common (undocumented). **(needs review)** |
| 9 | `CompressionPipeline`/`Builder` — internal-test-only, not in PyO3 | crates/.../pipeline/orchestrator.rs, mod.rs:62 | 120 | yagni | deadcode-rust | Documented future integration point — keep, or remove pub re-export if Phase C cancelled. **(needs review)** |
| 10 | `TOINBackend` Protocol — single FileSystemTOINBackend impl | telemetry/backends/base.py:15-54 | 40-55 | yagni | abstraction-bloat | Inline or structural-type; audit toin.py constructor `backend` param (tests). **(needs review)** |
| 11 | Three duplicate `TokenCounter` Protocol defs | providers/base.py, tokenizers/base.py, compression_units.py | 30 | dup | over-abstraction | Consolidate to tokenizers/base.py (cascades with Provider ABC removal). **(needs review)** |
| 12 | `CompressionPolicy` Rust plumbed-but-unconsumed fields (volatile_token_threshold, max_lossy_ratio, toin_read_only) + dead methods | crates/.../compression_policy.rs | 60 | yagni | deadcode-rust | **HARD INVARIANT:** Rust↔Python parity test pins all 5 field names — remove matching Python `CompressionPolicy` fields + update parity test in same atomic commit. F2.2/F2.3 decision needed. **(needs review)** |
| 13 | `live_zone_only` / semantic-cache fields on CompressionPolicy/CacheConfig | transforms/compression_policy.py, cache/base.py | ~15 | deadflag | over-abstraction | Same parity-test invariant for compression_policy fields. semantic_cache fields panel ✓ (Tier-1-able). **(needs review)** |
| 14 | `ast-grep-cli` dead core dep (CodeCompressor uses tree-sitter; binary wheel downloaded for nothing) | pyproject.toml:51 | 1+wheel | dep | dep-bloat | Remove line; `rg` confirms 0 subprocess/sg invocations. panel ✓✓✗ **(needs review)** |
| 15 | Amputated `progress`/`rich` extra (consumer binaries.py archived) | pyproject.toml:84-87,99-101 | 5 | deadflag | dep-bloat | Delete block + drop from `all` umbrella. panel ✓✓✓ |
| 16 | Rust `tokenizers` 0.22 + `hf-hub` 0.4 power only test-reachable HF backend (lookup_hf always None at runtime) | crates/.../tokenizer/hf_impl.rs + registry HF fns + Cargo.toml:24,30 | 372 | dep | dep-bloat | Delete hf_impl.rs + registry HF plumbing + tests + mod.rs re-exports, then drop 2 Cargo deps; `cargo check`. **(needs review)** |
| 17 | API-surface over-export shrink: relevance cluster, CompressionCache, _HTML_EXTRACTOR_AVAILABLE, MCP_SERVER_AVAILABLE, batch/context-tracker/streaming __all__ clusters | headroom/__init__.py, cache/__init__.py, ccr/__init__.py, transforms/__init__.py | ~90 | shrink | api-surface | Mostly `__all__`/_LAZY_EXPORTS pruning; update docs to submodule-direct imports first. `_HTML_EXTRACTOR_AVAILABLE` panel ✓✓✓ (Tier-1-able). Most others **(needs review — product decision: is X a public extension point?)** |
| 18 | `tomli` mis-classified as core dep (only reached in editable git checkout, never in wheel) | pyproject.toml:52, release_version.py | 1 | dep | dep-bloat | Move to `dev` extra. **(needs review)** |

**Tier 3 note (LIVE — not pure removal):** Rows 1-2 are the honest "amputated bloat still load-bearing" cases. anchor_selector.py (770) is dead-in-practice but public-API-exported; the Py↔Rust hand-mirror (580) is fully live and parity-gated — it's *consolidatable*, not deletable. Do not count these in a "delete now" headline.

**Packaging hygiene (NOT cuts, est_loc_cut=0):** undeclared optional imports (spacy/sentence_transformers/psutil/mistral_common/zstandard/brotli — DEP001, missing-extra bug); `httpx` used by core but declared only in `mcp` extra (unguarded import in batch_processor.py:28). Inverse of bloat — flagged so the synthesizer doesn't mistake them for removable.

---

## COMPLEXITY-LINT QUARANTINE
### OPTIONAL — high-churn, regression-risk on CCR/parity/cache invariants, NOT recommended. NOT a cut tier.

Churning 154 magic-values across a hardened, parity-tested compression engine is the opposite of lazy-senior. These touch live arithmetic on CCR keys, cache TTLs, and Rust↔Python parity. Listed for completeness only; do not action without a dedicated, separately-gated, fully-tested pass.

| Rule | Count | What | Why quarantined |
|------|-------|------|-----------------|
| PLR2004 | 154 | magic-value-comparison | 154 edits across hardened engine; regression risk on thresholds/ratios |
| C901 | 53 | complex-structure | structural refactor, high blast radius |
| PLR0912 | 36 | too-many-branches | dispatch logic; churn risk |
| PLR0913 | 24 | too-many-arguments | signature churn → call-site cascade |
| PLR0915 | 24 | too-many-statements | function-splitting churn |
| PLR0911 | 14 | too-many-returns | control-flow churn |
| RUF012 | 8 | mutable-class-default | semantic-change risk |
| SIM102/108/114/118/… | ~30 | collapsible-if / if-exp / dict-lookup | cosmetic; touches live branches |
| RUF022 | 14 | unsorted `__all__` | cosmetic; auto-fixable but reorders public surface |

(RUF100/ERA001/F401/PLR1711 are NOT here — they are genuine dead-code/hygiene cuts, placed in Tier 1.)

---

## What v2 found that v1 MISSED (be honest)
- **The single biggest code lever (live_zone ~2000 LOC) is invisible to every single-language lens.** Cargo treats `pub fn` as crate-API "used"; vulture/ruff are Python-only. Only the cross-language `dup-py-rust` (critic) lens caught that the whole-request compressors are test-only + a dead PyO3 bridge. v1 could not have seen this.
- **Dead `_core` PyO3 surface** (`hello`, `is_json_array_of_dicts` registration, responses bridge) — same cross-language blind spot.
- **The Py↔Rust hand-mirror (compression_policy.py + adaptive_sizer.py, ~580 LOC)** is *maintenance-coupling fat*, not dead twin: both halves LIVE, kept in sync by hand, policed by 2 parity suites. **Honest verdict: the amputated bloat is still load-bearing — 0 deletable now, consolidatable only via PyO3.**
- **anchor_selector.py (770 LOC)** Python impl is dead-in-practice (SmartCrusher is Rust-backed) but survives via public `__all__` — a lens that only checks internal call graphs would mark it "used."
- **Generated run-OUTPUTS (~1.45 MB)** and **empty phantom dirs** — no code lens catches committed artifacts or zero-byte scaffolding.
- **Worktree reconciliation:** v2 verified the 19 GB worktree is already cleaned (0 B), correcting the stale "19 GB live" framing.

## Refuted Appendix
**Panel net-refuted 0 findings.** No finding carries `survives:false`. The following carried a single dissenting "refuted" vote among confirms but SURVIVED (kept in-tier, flagged "(needs review)"): `summarize_dropped_items`, `route_and_compress`, `pipeline.simulate`, `eager_load_compressors`, `HybridScorer.has_embedding_support`, semantic_cache CacheConfig fields, `_reset_*_for_test` stubs, `unload_kompress_model`, `TelemetryCollector.record_compression`, `verify/heldout/measure.py` dup, `ast-grep-cli` dep, `sqlite_default`, `chars_per_token`, `model/encoding_name`, `KeywordDetector.registry`, several RUF100/F401 test-import rows, `wrap gemini` doc fix, `get_recommendation` PLR1711. None were dropped — a lone refuted vote against multiple confirms plus tool evidence does not overturn a finding.

---

**This is REPORT-ONLY.** Nothing was modified (read-only auditor: no Edit/Write, no git mutation). Applying any row is a separate, gated **archive + test** step — each Tier 2/3 row requires its stated untangle, a fresh `cargo check`/`pytest` + parity-suite run, and (for public-API/exports) a changelog/deprecation decision before removal.