[toolchain]
# Pin one Rust toolchain version for local, CI, and devcontainer use. Keep every workflow/toolchain
# site synchronized through the dedicated pin test; drift can change compiler behavior and cache keys.
channel = "1.95.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
